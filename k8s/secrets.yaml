apiVersion: v1
kind: Secret
metadata:
  name: mysql-credentials
  namespace: sample-app
type: Opaque
stringData:
  # MySQL root password (used only by the DB itself)
  mysql-root-password: rootpassword
  # App DB user and password (used by MySQL and backend)
  db-user: appuser
  db-password: apppassword


